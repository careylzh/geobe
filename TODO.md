
* [x] most of this code base is still far from an actual prog lang, as there are no control structures, no programming paradigms (e.g. functional, oop, etc). execute skill-3 on how programming languages are generally written, and emulate the simplicity of python in this language. Write your findings in a new markdown. Done: added `LANGUAGE_DESIGN_FINDINGS.md` with a bounded source inventory, claim ledger, proposed grammar, and staged roadmap.
* [x] clean up the readme - right now, the shiniest feature is `geobe --console`, which allows the user to type lowercase ascii characters, see the corresponding geometric encodings, and upon pressing enter, see the english characters. I don'tthink this current feature counts as a prog lang, so clean up the readme accordingly, and feature this at the top of the readme after the introduction. Done: the README now leads with the console flow and accurately frames the interpreter as experimental rather than general-purpose.
* [ ] set up a github pages which has a text field, boots up with `geobe --console` and allows the user to type and press enter. use `https://github.com/zensical/zensical`.
